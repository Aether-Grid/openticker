# Momentum Ultima Plus bot template.

id = "equities-basket-momentum-ultima-plus-paper"
enabled = true
allow_live = false
market = "equities"
symbols = ["AAPL", "MSFT", "COIN", "PLTR", "TSLA", "CCL", "CZR"]
# Each symbol runs as an independent runtime lane under this bot.
timeframe = "5m"
account = "alpaca-paper"
data_connector = "alpaca"
execution_connector = "alpaca"
strategy = "single_indicator_signal"
signal_mode = "confirmed_only"
polling_enabled = true
polling_interval_ms = 1000

# Optional warmup override.
# warmup_target_bars = 200

[[indicators]]
id = "ultima-1"
type = "momentum_ultima_plus"
enabled = true
role = "primary_signal"
signal_policy = "confirmed_required"

# Optional metadata-aware trade gating.
[indicators.metadata_filters.entry]
# allowed_strengths = ["strong"]
# allowed_reason_codes = ["supertrend_cross_up_sma_filter"]
# required_tags_any = ["buy", "strong"]

[indicators.metadata_filters.exit]
# allowed_strengths = ["normal", "strong"]
# allowed_reason_codes = ["supertrend_cross_down_sma_filter"]
# required_tags_any = ["sell"]

[indicators.params]
# Runtime-recognized for momentum_ultima_plus.
sensitivity = 4.0

[budget]
# Shared across all lanes for this bot.
pct = 10.0

[risk]
profile = "equities-default"

[risk.overrides]

stale_data_ms = 30000
# target_order_notional_usd = 2000.0
# max_order_notional_usd = 2500.0

[execution_constraints]
# quantity_step = 1.0
# min_quantity = 1.0
# min_notional_usd = 10.0
